import Swagger from 'swagger-client';

Swagger.http()
// http://topgear.prepub.souche.com/api-docs/
const request = {
    url,
    query,
    method,
    body,
    headers,
    requestInterceptor,
    responseInterceptor,
    userFetch
  }
